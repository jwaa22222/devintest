# Server configuration
PORT=4000

# Secret used to sign JWT auth tokens. Change this to a long random string.
JWT_SECRET=please-change-this-to-a-long-random-secret

# Comma-separated list of allowed CORS origins (the frontend dev server)
CLIENT_ORIGIN=http://localhost:5173
